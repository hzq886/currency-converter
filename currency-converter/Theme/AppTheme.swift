import SwiftUI

enum AppTheme {
    // MARK: - Colors
    static let background = Color(red: 0.08, green: 0.08, blue: 0.12)
    static let cardBackground = Color(red: 0.12, green: 0.12, blue: 0.18)
    static let buttonBackground = Color(red: 0.15, green: 0.15, blue: 0.22)
    static let buttonPressed = Color(red: 0.18, green: 0.18, blue: 0.26)
    static let accent = Color(red: 0.55, green: 0.35, blue: 0.85)
    static let accentBright = Color(red: 0.65, green: 0.45, blue: 0.95)
    static let textPrimary = Color.white
    static let textSecondary = Color(white: 0.55)
    static let divider = Color(white: 0.2)

    // MARK: - Dimensions
    static let keypadButtonSize: CGFloat = 68
    static let keypadSpacing: CGFloat = 14
    static let flagSize: CGFloat = 36
    static let cornerRadius: CGFloat = 16
}
